pub fn brackets_are_balanced(string: &str) -> bool {
    let mut list characters;

    match character {
        '{' => expr, //add to list
        '[' => expr, //add to list
        '(' => expr, //add to list
        '}' => expr, //pop from list
        ']' => expr, //pop from list
        ')' => expr, //pop from list
        _ => expr,
    }

    if characters.len() != 0 {
        false
    } else {
        true
    }
}
