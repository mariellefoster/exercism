def is_valid(isbn):
    
    # print(isbn)
    # print([int(isbn[i]) * (10-i) for i in range(len(str(isbn)))])
    # print(sum([int(isbn[i]) * (10-i) for i in range(len(str(isbn)))]))

    # return sum([if isbn[i].isnumber() : isbn[i] * (10-i) for i in range(len(str(isbn)))]) % 11 == 0
